# dhcpd.conf
#
# Sample configuration file for ISC dhcpd
#
# Attention: If /etc/ltsp/dhcpd.conf exists, that will be used as
# configuration file instead of this file.
#

# option definitions common to all supported networks...
option domain-name "example.org";
option domain-name-servers ns1.example.org, ns2.example.org;

default-lease-time 600; # seconds
max-lease-time 7200;    # seconds

# The ddns-updates-style parameter controls whether or not the server will
# attempt to do a DNS update when a lease is confirmed. We default to the
# behavior of the version 2 packages ('none', since DHCP v2 didn't
# have support for DDNS.)
ddns-update-style none;

option ip-tftp-server code 150 = { ip-address };

subnet 192.168.56.0 netmask 255.255.255.0 {
    range 192.168.56.5 192.168.56.10;
    option domain-name-servers 8.8.8.8;
    option domain-name "my-cloud.orz";
    option subnet-mask 255.255.255.0;
    option routers 192.168.56.2;  # dhcp server ip address
    option broadcast-address 192.168.56.254;
    option tftp-server-name "my_tftpd";
    option ip-tftp-server 192.168.56.3;
    next-server 192.168.56.3;
    default-lease-time 300;
    max-lease-time 900;
    filename "ggg.jpg";
}
